 
Libraries for MINGW/Windows:
    pacman -S mingw-w64-x86_64-glew
    pacman -S mingw-w64-x86_64-SDL2

Libraries for Liunx:
    apt install g++
    apt install cmake
    apt install libsdl2-dev
    apt install libglew-dev
